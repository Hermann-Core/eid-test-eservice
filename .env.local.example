
# eID-Server Configuration
EID_SERVER_URL=https://localhost:8443/eIDService
EID_SERVER_ADDRESS=https://localhost:8443/eIDService

# Application Configuration
NEXT_PUBLIC_BASE_URL=https://localhost:3000
PORT=3000

# ==========================================
# HTTPS SERVER CONFIGURATION
# ==========================================

# HTTPS Server Certificates (for eService itself)
# HTTPS_CERT_PATH=./certs/server.crt
# HTTPS_KEY_PATH=./certs/server.key
# HTTPS_CA_PATH=./certs/ca.crt  # Optional

# ==========================================
# EID-SERVER TLS CONFIGURATION
# ==========================================

# TLS Mode: 'normal' for regular TLS, 'mtls' for mutual TLS
EID_SERVER_TLS_MODE=normal

# Option 1: Certificate File Paths (RECOMMENDED)
# EID_SERVER_CERT_PATH=./certs/client.crt
# EID_SERVER_KEY_PATH=./certs/client.key
# EID_SERVER_CA_PATH=./certs/server-ca.crt  # Optional

# Option 2: Direct PEM Content (Alternative)
# EID_SERVER_CERT=-----BEGIN CERTIFICATE-----\n...
# EID_SERVER_KEY=-----BEGIN PRIVATE KEY-----\n...
# EID_SERVER_CA=-----BEGIN CERTIFICATE-----\n...

# ==========================================
# SECURITY CONFIGURATION
# ==========================================

# Accept self-signed certificates (set to 'false' in production)
NODE_ENV=development

# Certificate validation (set to 'true' in production)
# EID_SERVER_REJECT_UNAUTHORIZED=false
>>>>>>> REPLACE
</diff>
</apply_diff>